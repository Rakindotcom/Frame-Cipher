[build]
  command = "npm run build"
  publish = ".next"

# Pin Node to v22 LTS for reproducible build environment.
[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
